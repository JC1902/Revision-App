<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ProyectoFinal" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="MyFloatingActionButtonStyle" parent="Widget.MaterialComponents.FloatingActionButton">
        <!-- Cambia los atributos según tus preferencias -->
        <item name="android:backgroundTint">#FF00AA</item>
        <item name="android:width">2dp</item>
        <item name="android:elevation">6dp</item>
        <!-- Otros atributos de estilo si es necesario -->
    </style>

    <style name="Theme.ProyectoFinal" parent="Base.Theme.ProyectoFinal" />
</resources>